<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_color"
    tools:context=".MainActivity"
    tools:ignore="MergeRootFrame" >
    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:layout_centerInParent="true"
        android:paddingBottom="@dimen/vertical_offset"
        android:orientation="vertical">
        <TextView
            android:id="@+id/lbl_clock"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:padding="@dimen/btn_margin"
            android:textColor="@color/font_color"
            android:textSize="@dimen/main_font_size"
            android:gravity="center"
            android:text="@string/init_str"/>
        <ImageButton
            android:id="@+id/btn_start_stop"
            android:layout_width="@dimen/btn_width"
            android:layout_height="@dimen/btn_height"
            android:layout_marginTop="@dimen/btn_margin"
            android:background="@color/btn_color"
            android:layout_gravity="center"
            android:textColor="@color/font_color"
            android:tag="0"
            android:onClick="btn_start_stop"
            android:src="@drawable/play"/>
    </LinearLayout>

    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="@dimen/container_margin"
        android:orientation="horizontal">
        <ImageButton
            android:layout_width="@dimen/btn_width"
            android:layout_height="@dimen/btn_height"
            android:background="@color/bg_color"
            android:src="@drawable/about"
            android:onClick="btn_aboutClick"/>
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_weight="1" />
        <ImageButton
            android:layout_width="@dimen/btn_width"
            android:layout_height="@dimen/btn_height"
            android:background="@color/bg_color"
            android:src="@drawable/setting"
            android:onClick="btn_settingClick"/>
    </LinearLayout>
</RelativeLayout>
